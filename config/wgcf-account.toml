# Do not store real WARP credentials under config/.
# If you want to reuse an existing account, place it at:
#   ./data/wgcf-account.toml
# Leave that file absent to let the container auto-register at first start.
